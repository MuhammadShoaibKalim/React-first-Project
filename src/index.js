import React from 'react';
import ReactDOM from 'react-dom';
import App from './TodoList'; // Make sure the import points to your App component
import './index.css'; // Correct the import path

ReactDOM.render(
  <App />,
  document.getElementById("root")
);

